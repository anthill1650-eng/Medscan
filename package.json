{
  "name": "medscan",
  "private": true,
  "packageManager": "pnpm@8.10.0",
  "scripts": {
    "dev": "pnpm --filter api dev & pnpm --filter mobile start",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.2",
    "prettier": "^3.1.0"
  }
}
